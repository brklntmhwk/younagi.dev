---
type Props = {
  date: Date
}

const { date } = Astro.props
---

<time datetime={date.toISOString()}>
  {
    new Intl.DateTimeFormat('en-us', {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
    }).format(date)
  }
</time>
